install.packages("tidyverse", dep = TRUE)

library(tidyverse)

install.packages("magic", dep = TRUE)

library(magic)

#Crea un cuadrado de numeros que suman lo mismo en todos los sentidos
magic(6)

installed.packages()